---
description: Cursor 作為本 repo 的 AI orchestrator 時的編排規則（demo 實例 · v4 local-first）
globs:
  - "**/*"
alwaysApply: true
---

# Orchestrator Rules（v4 local-first）

本專案 **Cursor IDE 為唯一主編排者**。本頻道為 **demo 唯讀博物館**；活躍專案請用各產品 `#10-proj-*`。

## Workflow v4

1. 任務：`docs/tasks/T-*.md`；進度：`docs/progress.md` → **git push**。
2. **本機**執行；預設 Slack **不** `[handoff]` @ bot。
3. 可選 `[status]` 鏡像；skills 原始碼在 `.cursor/skills/`。

## 執行優先順序

1. 讀 `docs/WORKFLOW-v4.md`、`docs/progress.md`。
2. `gh` 更新 issue / branch / PR / progress。
3. 本機實作；可選 Slack `[status]`（無 mention）。

## 禁止假設

- Slack execute 自動排隊、YAML 自動部署、MCP `/invite`。
- 其他專案複製 demo `docs/`（只 copy skills + bootstrap）。

## 契約檔

- `docs/WORKFLOW-v4.md`
- `docs/progress.md`
- `.cursor/skills/ai-orchestrator/`

## 本專案 Slack（demo）

- **頻道**：`#11-proj-ai-orchestrator-workflow-demo`（`C0B40L36REE`）— 唯讀封存
- **Repo**：`chang180/ai-orchestrator-workflow-demo`
